@model IEnumerable<comercializadora.DataBase.Lotes>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Extencion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TipoSuelo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TipoRiego)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CantidadCosecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Finca.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Extencion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TipoSuelo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TipoRiego)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CantidadCosecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Finca.Nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LoteId }) |
            @Html.ActionLink("Details", "Details", new { id=item.LoteId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LoteId })
        </td>
    </tr>
}

</table>
